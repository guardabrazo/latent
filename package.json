{
  "name": "latent-visualization",
  "private": false,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "rm -rf dist && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages --dist dist --branch gh-pages --message \"Deploy to GitHub Pages\" --dotfiles"
  },
  "devDependencies": {
    "@types/three": "^0.176.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.176.0"
  }
}
